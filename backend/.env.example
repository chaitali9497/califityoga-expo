PORT=5000
# Use a remote MongoDB URI when deployed (MongoDB Atlas, Render database, etc.)
MONGODB_URI=mongodb://localhost:27017/califitoga
# Set a strong JWT secret in production / deploy environments
JWT_SECRET=your_jwt_secret_key_here_change_in_production
NODE_ENV=development
